import React from 'react';
import ReactDOM from 'react-dom';
import CertificateView from './components/CertificateView'; // Adjust the path as needed
import './index.css'; // Import your styles if applicable

ReactDOM.render(
  <React.StrictMode>
    <CertificateView />
  </React.StrictMode>,
  document.getElementById('root')
);
